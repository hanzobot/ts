apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  labels:
    app: openclaw
data:
  openclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.hanzo/bot/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Hanzo Bot Assistant",
            "workspace": "~/.hanzo/bot/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Hanzo Bot Assistant

    You are a helpful AI assistant running in Kubernetes.
